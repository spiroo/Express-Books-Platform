<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>example - page2</title>
    <% if (env === "production") { %>
        <link rel="stylesheet" href="/css/user.css">
    <% } %>
</head>
<body>
    <div class="total-container">
        <div class="first-note">This is user.</div>
        <br>
        <div class="image-example-common"></div>
    </div>
    <script src="/public/js/user.js"></script>
    <% if (env !== "production" && reload === true) { %>
        <script src="/reload/reload.js"></script>
    <% } %>
</body>
</html>
